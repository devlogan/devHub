import React, { Fragment } from "react";
import loader from "../../img/loader.gif";

export default () => (
  <Fragment>
    <img src={loader} style={{ margin: "auto", display: "block" }} />
  </Fragment>
);
